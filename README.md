# massage-backend-api

## Quick start

1. Install dependencies:
   `npm install`
2. Copy environment template:
   `cp .env.example .env`
3. Run in development mode:
   `npm run start:dev`

## Useful commands

- Run tests: `npm test`
- Lint code: `npm run lint`
- Build: `npm run build`